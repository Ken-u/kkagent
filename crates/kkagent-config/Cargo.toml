[package]
name = "kkagent-config"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
toml_edit = { workspace = true }
dotenvy = { workspace = true }
dirs = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
# Used by `install_test_home!` so every crate's test binary can redirect
# `default_config_dir()` away from the real `~/.kkagent` before tests run.
ctor = "0.2"

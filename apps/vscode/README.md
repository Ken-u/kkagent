# kkagent VS Code extension

Minimal IDE bridge:

- `kkagent: Start ACP bridge` — spawns `kkagent acp`
- `kkagent: Send prompt` — posts to HTTP API or ACP stdio

Configure `kkagent.binary` and `kkagent.httpUrl` in settings.
